# EwE School Website — текущее состояние

Снимок на 2026-07-22. Не лог сессий — старое вытесняется новым.

## 🟢 Задеплоено на прод 2026-07-22 (коммит `b36cb3e`)

Проверено на живом сайте: сборщик атрибуции в бандле, заявка пишется в
`/var/lib/eweschool/leads.jsonl`, тестовая запись удалена (бэкап рядом).

⚠️ Push в GitHub прод **не** обновляет — DNS смотрит на Timeweb, не на Vercel.
Обязательный второй шаг после каждого push:
```bash
ssh -J root@204.168.171.55 root@10.20.0.1 \
  'su - deploy -c "cd ~/apps/english-school-website && ./deploy.sh"'
```

## 🟢 Сделано (2026-07-22) — измерения маркетинга

- **Сбор UTM** (`lib/attribution.ts` + `components/attribution-tracker.tsx`, кука `ewe_attr`, 90 дней).
  Хранит `first` и `last` touch: first-touch один занижает вклад рекламы, last-touch завышает.
  Ловит `utm_*`, `yclid`, referrer, landing page, ClientID Метрики.
  Трекер смонтирован в `app/[lang]/layout.tsx` — параметры приходят на первую страницу,
  а форма открывается позже, к тому моменту их в адресе уже нет.
- **Журнал заявок** (`app/api/enroll/route.ts`). Порядок изменён: сначала запись в
  `LEADS_FILE`, потом Telegram и почта. Если журнал не записался — заявка не принята.
  Причина: 152-ФЗ (первичная запись ПДн в РФ) и невозможность считать заявки, пролистывая чат.
- **Согласие проверяется на сервере** — форма шлёт `agree`, API без него отклоняет.
- **Токен бота убран из кода.** Был захардкожен как fallback в `route.ts:4-5`.

## 🔵 Blocked — нужен Армен

1. **Проверить в панели Метрики, создана ли цель `enroll_submit`.** Вызов `reachGoal`
   есть в `app/[lang]/enroll/success/page.tsx`, но если цели нет в интерфейсе — она никуда
   не пишется, и все прошлые конверсии не считались.
2. **Перевыпустить токен бота в @BotFather** — старый лежит в истории git открытым текстом.
   Смена токена ломает прод, пока новый не проставлен в `.env.production` на сервере.
3. **Решить, куда шлём лиды.** На сервере `TELEGRAM_CHAT_ID=301635514` — это личка,
   а не группа «EWE Лиды» (`-5136336432`). Заявки мешаются с крипто- и тендер-пушами.

## 📌 Инфраструктура

- Прод: TimeWeb `5.129.252.36`, **не Vercel** (DNS смотрит на Timeweb)
- Код на сервере: `/home/deploy/apps/english-school-website`, PM2 `ewe-website`, порт 3000
- Журнал заявок: `/var/lib/eweschool/leads.jsonl` (владелец `deploy`, права 750).
  Каталог создан, `LEADS_FILE` добавлен в `.env.production` — **сервер готов, ждёт код**
- В dev журнал пишется в `.data/leads.jsonl` (в `.gitignore` — там ПДн)
- Метрика `107144239`, константа `METRIKA_ID` в `lib/attribution.ts`
